#include<stdio.h>
#include"3x3.h"
#include"3x3.h"

int main()
{
    int low, high;
    printf("enter low of the range: "); scanf("%d",&low);
    printf("\nenter high of the range: "); scanf("%d",&high);
    m_tabale(low, high);
    getchar();getchar();
    return 0;
}
